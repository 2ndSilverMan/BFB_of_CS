name: docs

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate Markdown docs
        run: python Maintainers/Scripts/validate_docs.py

      - name: Check summary counts are in sync
        run: python Maintainers/Scripts/sync_summary_counts.py --check

      - name: Run maintainer script unit tests
        run: python Maintainers/Scripts/test_validate_docs.py
