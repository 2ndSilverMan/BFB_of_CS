# 주제명 (Topic Name)

> 이 템플릿은 **deep-dive(자기완결 심화) 주제 문서** 전용이다.
> 기본 주제 문서는 [Topic-Template.md](Topic-Template.md)를 쓴다. 두 tier의 차이와 운영 원칙은 [CONTRIBUTING.md](../CONTRIBUTING.md)의 "문서 깊이(Depth) tier"를 따른다.
> deep-dive는 모든 문서가 아니라 **소수 핵심·길목 주제**에만 선별 적용한다(검토 비용 때문).

- Level: Intermediate
- Prerequisites: 없음
- Status: Stub
- Reviewed-by: -
- Depth: Deep-dive (자기완결)

---

<!--
Depth: Deep-dive 의 품질 바 (Status를 Draft 이상으로 올리기 전 자가 점검):
1) 선언한 선수지식 위에서 자기완결적이다 — 이 문서만으로 메커니즘을 이해할 수 있다.
2) 이론/구현/복잡도가 "무엇"을 넘어 "왜·어떻게"(메커니즘)를 담는다.
3) 최소 1개의 워크드 예제(수치 계산 또는 구체 사례)가 있다.
4) 구현은 실제로 실행 가능한 명령·코드·설정이다(의사코드만으로 끝내지 않는다).
5) 실전 실패 모드/함정을 다룬다.
6) 구조나 흐름은 ```mermaid``` 다이어그램으로 보여 준다.
7) 비자명한 사실 주장에는 참조가 붙는다.

사람 검토를 마쳤다면 Status를 올리고, Reviewed-by를 "이름 (YYYY-MM-DD)"로 채운 뒤
본문 맨 위에 배지 한 줄을 추가한다(검토 안 했으면 넣지 않는다):
> ✅ **사람 검토 완료** — 이름, YYYY-MM-DD
deep-dive는 검증할 사실이 많으니 Complete 승격 전 검토를 특히 꼼꼼히 한다.
-->

## 개념 (Concept)

핵심 개념을 한 문단으로 정의하고, 인접 개념과의 **경계(무엇이 아닌지)** 를 분명히 한다.

## 직관 (Intuition)

왜 이런 구조/방법이 필요한지. 가능하면 멘탈 모델을 1~2개 제시하고 ```mermaid```로 구조를 보여 준다.

## 이론 (Theory)

deep-dive의 본문. 메커니즘을 끝까지 설명한다.

- 정의·수식·증명은 GitHub LaTeX. 인라인 `$ ... $`, 블록 `$$ ... $$`.
- 핵심 메커니즘을 단계로 분해하고, 설계 트레이드오프(대안과 비교)를 표로 정리한다.
- 가장자리 사례(edge case)와 가정이 깨질 때 무엇이 무너지는지 밝힌다.

## 구현 (Implementation)

```text
실제 실행 가능한 명령·코드·설정 예시. 핵심 경로의 의사코드도 함께 둘 수 있다.
```

## 복잡도 (Complexity)

알고리즘 주제면 시간/공간 표를, 시스템/운영 주제면 비용·성능·실패 특성 표를 쓴다.
**워크드 예제**(수치 대입)를 최소 하나 포함한다.

## 응용 (Applications)

실제로 어디서, 왜 쓰이는가. 관련 문서로 교차 링크한다.

## 흔한 오해 (Common Misunderstandings)

실전에서 반복되는 잘못된 직관과, 왜 틀렸는지를 짝지어 적는다.

## TMI

- 본문 지식은 아니지만 알면 유용하거나 현업에서 마주치는 잡지식 2~4개.
- 역사·이름 유래·유명한 함정·디버깅 때 보는 메시지 등. 소문은 피하고 가능하면 참조를 남긴다.

## 연습 / 확인 문제 (Exercises)

- 개념을 직접 적용하는 계산형/설계형 문제. deep-dive는 워크드 예제를 응용한 수치 문제를 1개 이상 둔다.

## 이어서 읽기 (Reading Path)

- 이전:
- 다음:
- 관련:

## 참조 (References)

- 책/논문/공식 문서/강의 링크. 본문의 비자명한 주장과 연결한다.

## 법적/저작권 확인 (Legal Check)

- 외부 자료의 본문, 코드, 이미지, 표, 문제, 답안을 무단 복사하지 않았다.
- 필요한 출처는 참조에 남겼고, 불법 공유/우회 링크는 포함하지 않았다.
- 개인정보, API 키, 토큰, 비밀번호, 내부 URL이 없다.
